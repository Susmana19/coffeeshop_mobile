import { StyleSheet, Dimensions } from "react-native";

const style = StyleSheet.create({
    title: {
        fontSize: 34,
        fontWeight: '700',
        color: '#000000', 
        marginTop: 50   
    }   
})

export default style;